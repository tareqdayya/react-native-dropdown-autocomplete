import {StyleSheet} from "react-native";

export default StyleSheet.create({
  container: {
    borderRadius: 0,
    justifyContent: "center",
    flexDirection: 'row',
    flexWrap: 'wrap',
    paddingVertical: 12,
    paddingLeft: 6,
  },
});
